export enum Transport {
  STDIO = "stdio",
  SSE = "sse",
}

export enum Tool {
  TOOLS = "tools",
  PROMPTS = "prompts",
  STATIC_RESOURCES = "static-resources",
  DYNAMIC_RESOURCES = "dynamic-resources",
}
